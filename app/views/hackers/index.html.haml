%h2= t('users.title')

.container
  .row
    .col-lg-12
      = render 'hackers', hackers: @users_visible_for_all

  - if current_user.admin?
    %h2 Suspended, banned and forgotten
    .row
      .col-lg-12
        = render 'hackers', hackers: @suspended_banned_and_forgotten
